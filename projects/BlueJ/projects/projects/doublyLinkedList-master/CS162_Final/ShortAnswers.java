
/**
 * Write a description of class ShortAnswers here.
 *
 * @author Ryan Blizard
 * @version 0.0.1
 */
public class ShortAnswers
{
    /**
     * Answers here:
     * -------------
     * 1. a) A. b) B. c) A.
     * 2. Greatest to Least: 2^n, N^3, N^2, N*log(N), N, sqrt(N), log(N)
     * 3. In this order: O(1), O(N), O(N), O(N), O(1)
     * 4. public static double foo(double n, double b){
     *          double result = 0;
     *          if(n == 0){
     *              result = result + 1;
     *          } else {
     *              n = n-1;
     *              result = b * foo(n, b);
     *          }
     *       return result;
     *    }
     * 5. You can lower code duplication and shorten methods, but you can also
     *    create a large opportunity for overflow errors, and readaility can
     *    lower.
     * 6. {0,1,4,9,6,3,5,2,7,8}
     * 7. O(N^2)
     * 8. Preorder: {15,8,1,-3,3,10,33,27,30,51}. 
     *    Postorder: {-3,3,1,10,8,30,27,51,33,15}.
     * 9. O(log(N))
     * 10. a.
     * 11. c.
     * 12. e.
     * 13. The first would because x contains a Y object, but the second won't,
     *     since the compiler would tell you that information would be lost 
     *     from x.
     * 14. b.
     * 15. 11011000
     * 16. -27
     * 17. IW
     * 18. 1000001
     * 19. 1100010
     * 20. private
     * 
     */
}
